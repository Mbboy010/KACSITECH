specVersion: "v1beta"
serviceId: "next-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "next-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
