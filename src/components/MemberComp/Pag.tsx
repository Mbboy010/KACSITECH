"use client"

import {useState,useEffect} from "react"


interface props{
  paginate: () => void;
  currentPage: number;
  setCurrentPage: React.Dispatch<React.SetStateAction<number>>;
  setCondition: React.Dispatch<React.SetStateAction<boolean>>;
  pagNumber: array;
  total: number;
}


export default function Pag({paginate,currentPage,setCurrentPage,pagNumber,total,setCondition}:props){
  
  
  
  
  
  
  
  
  return(

    )
}